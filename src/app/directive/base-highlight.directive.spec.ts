import { BaseHighlightDirective } from './base-highlight.directive';

describe('BaseHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
